# 设置页默认显示诊断面板（Stage 81）

## 变更目的
用户不希望再通过 `?diag=1` 才能查看诊断信息，希望在设置页直接可见并可复制。

## 代码变更
- `MFCMouseEffect/WebUI/app.js`
  - 将 `diagMode` 固定为 `true`，使诊断面板与诊断轮询在设置页默认启用。

## 影响说明
- 打开设置页即可看到 `Diag Stream` 与状态快照区。
- 保留现有“复制、清空、检查符号、Dump state”按钮行为。
- 不影响渲染路径，仅影响前端可观测性。
